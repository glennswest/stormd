[package]
name = "stormlog"
version = "0.3.0"
edition.workspace = true
license.workspace = true
description = "A container's logging: a rotated file, the fleet's multicast group, and a stream to follow"

[dependencies]
# The wire format, shared with stormpump. See the crate's own README for why
# this is not written out again here.
stormcast = { git = "https://github.com/glennswest/stormcast", branch = "main" }
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
chrono.workspace = true
uuid.workspace = true
thiserror.workspace = true
anyhow.workspace = true
vt100.workspace = true
tokio-util.workspace = true

